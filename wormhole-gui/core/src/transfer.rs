//! File transfer over transit (Phase 3 — stub for now).
//!
//! Phase 1/2 don't implement file transfer; this module exists so session.rs
//! can dispatch to it once Phase 3 lands. The end-to-end flow is verified in
//! ../../wormhole-spike/src/main.rs (T1.4).

#[allow(dead_code)]
pub(crate) fn placeholder() {}
